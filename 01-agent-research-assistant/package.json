{
  "name": "research-agent",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "pnpm --dir server install && pnpm --dir client install",
    "dev": "concurrently -n server,client -c blue,green \"pnpm --dir server dev\" \"pnpm --dir client dev\"",
    "dev:server": "pnpm --dir server dev",
    "dev:client": "pnpm --dir client dev",
    "build": "pnpm --dir server build && pnpm --dir client build",
    "test": "pnpm --dir server test && pnpm --dir client test"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
